{
  "name": "flowstate",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "vercel-build": "npm run db:deploy --prefix server && npm run db:generate --prefix server && npm run build --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  }
}
